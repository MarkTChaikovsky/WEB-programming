/* 7. Написати програму, яка показуватиме на екран квадрат числа, введеного
користувачем. Користувач повинен сам вирішувати – вийти із програми або
продовжити введення. */
while (true) {
    let n = parseInt(prompt("Введіть число, яке піднести до квадрату: "))
    alert(`квадрат числа ${n} :     ${n**2}`)
    let answear = prompt("Продовжити?", "так")
    if (answear == "так") {continue;}
    else {break;}
}